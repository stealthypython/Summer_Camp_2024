print("Welcome to the Tip Calculator!! By Anzie!")
# Ask for bill amount 💰 
bill = float(input("What was the total bill? "))
# Ask for tip percentage 💵  
tip = int(input("What percentage tip would you like to give? 10, 12, or 15? "))
# Who are the "Splitters"? 👨 👩 
split = int(input("How many people to split the bill? "))
# Calculate the tip amount ✔️ 
tip_amount = bill * (tip / 100)  # Calculate the tip amount
total_amount = bill + tip_amount  # Calculate the total amount
split_amount = total_amount / split  # Calculate the amount per person
print(f"Each person should pay: ${split_amount:.2f}")  # Format the output to two decimal places
